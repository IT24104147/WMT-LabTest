{
  "name": "wmt-labtest",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "postinstall": "npm install --prefix backend && npm install --prefix frontend && npm run build --prefix frontend -- --outDir ../build --emptyOutDir",
    "build": "npm run build --prefix frontend -- --outDir ../build --emptyOutDir",
    "start": "npm start --prefix backend"
  },
  "engines": {
    "node": ">=20"
  }
}
